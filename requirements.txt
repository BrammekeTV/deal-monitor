# Core Discord library
discord.py>=2.3.2

# Vinted scraper (API-based, no browser required)
vinted_scraper>=3.1.0

# httpx is pulled in by vinted_scraper; pinned here for clarity
httpx[brotli]>=0.20.0

# Async SQLite
aiosqlite>=0.20.0

# YAML configuration parsing
PyYAML>=6.0.1

# Environment variable loading
python-dotenv>=1.0.0

# HTTP client (for webhook delivery)
aiohttp>=3.9.0

# Browser automation for Cardmarket scraping (Cloudflare bypass)
playwright>=1.40.0

# Camoufox: patched Firefox with anti-fingerprinting for reliable Cloudflare bypass
camoufox>=0.4.0

# HTML parsing for Cardmarket product pages
beautifulsoup4>=4.12.0

# Fuzzy string matching for learning database lookup
rapidfuzz>=3.0.0
